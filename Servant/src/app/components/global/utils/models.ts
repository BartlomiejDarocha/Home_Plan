export interface StandardAlertInterFace {
  header: string;
  content: string;
}

export interface ConfirmAlertInterFace {
  header: string;
  content: string;
  confirmButton: string;
  cancelButton: string;
}