import { FormsValidationDirective } from './forms-validation.directive';

describe('FormsValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FormsValidationDirective();
    expect(directive).toBeTruthy();
  });
});
