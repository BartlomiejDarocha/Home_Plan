import { CheckPasswordRepeatDirective } from './check-password-repeat.directive';

describe('CheckPasswordRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPasswordRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
